{
  "phase": "ai-802",
  "stack": "node-ts",
  "feature": "AI-802",
  "cleared": true,
  "enforced": true,
  "reportRef": "docs/quality/ai-802-gate.md",
  "notes": "AI-802 聊天语音输入（cherry-pick c56d36e 集成到 feat/ai-804 主线）：纯前端增强，零新增后端 API。新增 useSpeechDictation hook（封装浏览器 Web Speech API 实时英文听写：supported/listening/interim/start/stop + 卸载清理）+ 纯逻辑模块 speech-dictation.ts（getSpeechRecognitionCtor / isSpeechDictationSupported / applySpeechResult，可单测）+ ChatInner 接线（textarea 左侧 🎤 按钮，final 增量追加 input、interim 仅预览、不支持/非安全上下文按钮 disabled 并 tooltip 提示、绝不报错/不阻断键盘输入）+ Chat 命名空间 i18n 中英 3 键。集成提交另解决与主线的两处冲突：world.ts 保留 AI-801 coursesBefore + AI-802 speechFinal 双字段；backlog 合并后 AI-801/802/803/804 均 done。集成验证：前端 tsc 0；e2e tsc 0；前端 vitest 全量（含 speech-dictation 15 用例）全绿；i18n parity 通过；BDD/E2E chat-voice.feature 两场景已就绪，交 CI e2e job 兜底（user-accepted-ci）。",
  "gates": {
    "consistency": "PASSED (前端 tsc --noEmit 0；e2e tsc --noEmit 0；前端 vitest 全量（含 speech-dictation 15 用例）全绿；无后端改动；全栈契约无需对齐——本 feature 纯前端、识别结果仅作输入框文本，发送沿用既有 POST /api/ai/chat/messages)",
    "tests": "PASSED (user-accepted-ci: 前端 vitest 15/15 用例（speech-dictation 纯逻辑）全绿、全量通过、i18n 中英键齐备且 parity 校验通过；BDD/E2E chat-voice.feature 两场景（麦克风可见→listening→输入出现英文→发送；不支持时按钮 disabled）已就绪交 CI e2e job 验证)",
    "review": "PASSED (0 open：纯前端零后端改动；supported 探测含安全上下文校验（Firefox/非安全上下文自动降级）；continuous 模式 onend 自动重启避免「说一半停」、手动 stop 落停不重启；onerror 仅记 warn 不抛、不破坏输入；组件卸载 abort 识别器防泄漏；onFinal 经 ref 持有最新回调避免闭包过期；无新增 TODO/stub/死代码)",
    "optimization": "PASSED (0 open：核心逻辑抽到纯模块便于单测、hook 只负责副作用；无占位/调试代码；i18n 复用既有 listening 键、新增 3 键中英齐备；无魔法值（lang='en-US' 符合英语对话场景）)"
  }
}
