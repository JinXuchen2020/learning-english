{
  "phase": "ai-307",
  "stack": "node-ts",
  "gates": {
    "consistency": "PASSED (frontend tsc --noEmit 0 错误; next build 通过[/speech 路由编译, 9 个页面全生成]; vitest 59/59 全绿[含 speech.spec 13 case: mapBackendMascotExpr 全映射+降级, isSpeechSynthesisSupported, speakText 全分支]; e2e 16/16 全绿[含 AI-307 新增 speech-practice.feature 3 scenarios]; e2e tsc 0 错误; backend 复用 AI-303/306 端点无新后端代码, 既有 jest 436 tests 不退化)",
    "tests": "PASSED (unit: vitest 59/59[新增 speech.spec.ts 13 case: mapBackendMascotExpr(cheer→celebrating, encourage→encouraging, happy/thinking passthrough, null/undefined/unknown→happy); isSpeechSynthesisSupported(有synth→true/无→false); speakText(speak调用/lang设置/voice匹配/不匹配/no synth→false/抛错→false)] + 回归 speech-recorder.spec 17 + api.spec 11 + plan.spec 14 + logger.spec 4; e2e/bdd: speech-practice.feature 3 scenarios 全绿[打开见卡片+听钮 / 听→录→评→得星(星动画 SpeechCelebration 触发) / 弱发音鼓励反馈且不得星]; headless 麦克风经 launch flags(--use-fake-device-for-media-stream/--use-fake-ui-for-media-stream) + Object.defineProperty 注入 fake getUserMedia/MediaRecorder 解决 navigator.mediaDevices 只读致覆盖静默失效、真实 getUserMedia 抛 NotAllowedError 使录音无法进入 recording 态)",
    "review": "PASSED (0 open; 契约对齐[evaluateSpeech 字段 audio/wordId/referenceText/durationMs/userId 与后端 EvaluateSpeechDto 对齐, 返回 SpeechFeedback 与 AI-306 结构一致]; 表情映射防腐[mapBackendMascotExpr 隔离后端 happy|encourage|thinking|cheer 与前端 happy|thinking|celebrating|encouraging 差异, 未知表情降级 happy, 不污染 Mascot 组件]; TTS 降级[Web Speech API 无支持时安全 no-op 不抛不阻塞听钮]; 空安全[getAllWords 失败→loadError 友好提示, evaluateSpeech 失败→cardError, 均不崩溃]; 不越界[无新后端实体/端点/依赖, 纯前端消费既有 Word 与 AI-303 接口; 会话星星数本地 useState 不落库, 累计/任务联动属 AI-308 deferred; 不构建后端 TTS, AI-402 显式 deferred]; AuthGate 包裹, token 内存态经 TabNav 客户端导航保活, 规避 page.goto 重置 token)",
    "optimization": "PASSED (0 open; 纯逻辑 speech.ts 全纯函数[node 直覆, 零 React 依赖]; speakText 注入式 synth + SpeechSynthesisUtterance 全局缺失时 node-safe 兜底(createUtterance 返回 minimal object); 无 stub/占位[Web Speech 降级是真降级非 TODO]; 无裸 console[统一 logger]; 无调试残留; evaluateSpeech 复用 ApiError 解析且不复写 JSON Content-Type[fetch 自动带 multipart boundary]; SpeechRecorder onReset 回调供父组件同步清卡态, 无冗余状态)"
  },
  "cleared": true,
  "enforced": true,
  "reportRef": "docs/quality/ai-307-gate.md",
  "notes": "AI-307 把 M3 口语链(AI-302 录 / AI-303 评 / AI-306 反馈)组装成儿童可感知的 /speech 跟读闭环页: 听单词(浏览器原生 Web Speech API 朗读)→录音跟读(SpeechRecorder)→提交评分(evaluateSpeech multipart POST)→看反馈(分数/弱音素/吉祥物表情)→通过得星+庆祝动画。新增 src/lib/speech.ts(纯逻辑: mapBackendMascotExpr/isSpeechSynthesisSupported/speakText)、src/lib/types.ts(SpeechFeedback/SpeechLevel/EvaluateSpeechOptions)、src/lib/api.ts(evaluateSpeech + postFormData multipart)、src/app/speech/page.tsx(AuthGate 包裹 + 状态机 + SpeechCard/SpeechFeedbackPanel/SpeechComplete)、src/components/TabNav.tsx(Speak 标签)、修改 src/components/SpeechRecorder.tsx(新增 onReset 回调)。无新后端代码/实体(纯前端消费既有 Word 与 AI-303 接口, 既有 jest 436 tests 不退化)。AI-402(TTS 后端)未落地, 用浏览器原生 Web Speech API 作朗读降级, 显式 deferred。会话星星数为本地 useState 不落库(累计/任务联动属 AI-308)。E2E headless 麦克风: 经 launch flags 让真实 getUserMedia/MediaRecorder 在 headless Edge 工作 + Object.defineProperty 注入 fake getUserMedia/MediaRecorder(解决 navigator.mediaDevices 只读导致覆盖静默失效、真实 getUserMedia 抛 NotAllowedError 致录音无法进入 recording 态)。验收全绿: 听→录→评→反馈→得星 链路贯通, 星动画触发。"
}
