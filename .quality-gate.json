{
  "phase": "ai-802",
  "stack": "node-ts",
  "feature": "AI-802",
  "cleared": true,
  "enforced": true,
  "reportRef": "docs/quality/ai-802-gate.md",
  "notes": "AI-802 聊天语音输入（cherry-pick c56d36e 集成到 feat/ai-804 主线 + 覆盖率修复）：纯前端增强，零新增后端 API。新增 useSpeechDictation hook + 纯逻辑模块 speech-dictation.ts + ChatInner 接线 + Chat i18n 3 键（详见 docs/quality/ai-802-gate.md）。集成冲突处理：world.ts 保留 AI-801 coursesBefore + AI-802 speechFinal 双字段；backlog 合并后 AI-801/802/803/804 均 done。**覆盖率修复（本提交）**：CI test:cov 全量跑出 functions 88.96% < 90% 挂门槛。两处修复：① jest.config.js 追加 coveragePathIgnorePatterns ['[\\\\/]migrations[\\\\/]']（Windows 下 collectCoverageFrom 否定 glob !src/migrations/** 匹配失效，3 个迁移文件 20 个未覆盖函数泄漏，正则按绝对路径兜底，migrations 0/0 排除）；② 补 rewards.controller.spec.ts（12 例）+ ai-word-card.controller.spec.ts（7 例）——两控制器此前仅有 service spec、函数覆盖 0~9%，补齐后全局 funcs 91.86% ≥ 90%，jest --coverage exit 0，四门槛全过。修复亦修掉 api-stream.spec.ts:196 init?.signal 的 null 可赋值性（集成时发现）。",
  "gates": {
    "consistency": "PASSED (前端 tsc --noEmit 0；e2e tsc --noEmit 0；前端 vitest 全量（含 speech-dictation 15 用例）全绿；后端 server tsc --noEmit 0；后端 jest --coverage 927/927 全绿（含新增 rewards.controller 12 例 + ai-word-card.controller 7 例），全局 functions 91.86% ≥ 90% 门槛，jest exit 0；i18n parity 通过)",
    "tests": "PASSED (user-accepted-ci: 前端 vitest 15/15（speech-dictation）+ 全量通过；后端 jest 927/927 全绿——覆盖修复新增 rewards.controller.spec 12 例（list/summary/myRedemptions/redeem/pending 带与不带 status/approve/reject 带与不带 reason/create/update/remove 转发断言）+ ai-word-card.controller.spec 7 例（generate 转发、list 合法/非法 status、approve/reject reviewerNote 转发与 undefined 兜底）；jest --coverage 四门槛全过（funcs 91.86%）；BDD/E2E chat-voice.feature 两场景交 CI e2e job 验证)",
    "review": "PASSED (0 open：纯前端零后端改动；supported 探测含安全上下文校验（Firefox/非安全上下文自动降级）；continuous 模式 onend 自动重启避免「说一半停」、手动 stop 落停不重启；onerror 仅记 warn 不抛、不破坏输入；组件卸载 abort 识别器防泄漏；onFinal 经 ref 持有最新回调避免闭包过期；无新增 TODO/stub/死代码)",
    "optimization": "PASSED (0 open：核心逻辑抽到纯模块便于单测、hook 只负责副作用；无占位/调试代码；i18n 复用既有 listening 键、新增 3 键中英齐备；无魔法值（lang='en-US' 符合英语对话场景）)"
  }
}
