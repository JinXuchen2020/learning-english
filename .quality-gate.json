{
  "phase": "feature",
  "stack": "node-ts",
  "cleared": true,
  "enforced": true,
  "reportRef": "docs/quality/ai-713-gate.md",
  "notes": "AI-713 收尾 + CI e2e 9 场景修复 + 绘本 AI 降级收紧：e2e 对 plan/scan/picture-book 三端点补 page.route mock（与既有 chat/speech/report 约定一致），parent panel 竞态修复（open 前等 le_auth_user.role==='parent'）；scan.service.recognize 包 try/catch 失败降级而非 500；ChatOptions 新增可选 maxAttempts，RetryableAiProvider 合并调用级覆盖，绘本 callAgent 传 {timeoutMs:20000,maxAttempts:1} 使智谱不可达约 20s 即落模板绘本（原 3x60s≈180s）；新增 retryable/picture-book 单测锁定行为。后端 tsc 0/0、前端 tsc 0/0、e2e tsx 仅转译；后端 jest retryable-ai-provider + picture-book.service 21/21 全绿。",
  "gates": {
    "consistency": "PASSED (后端 tsc -p tsconfig.json --noEmit 0/0；前端 tsc 0/0；e2e tsx 仅转译不类型检查；ChatOptions.maxAttempts 为向后兼容可选字段；RetryableAiProvider.resolveRetryOptions 仅覆盖 maxAttempts，其余退避参数沿用全局默认；.quality-gate.json 随源码一并暂存)",
    "tests": "PASSED (后端 jest retryable-ai-provider + picture-book.service 共 21/21 全绿，含 per-call maxAttempts=1 不重试与绘本 chat 选项断言；AI-713 触及 spec 133/133 仍有效；e2e 三类端点 mock 由 page.route 确定性夹具驱动，不依赖真实 LLM)",
    "review": "PASSED (0 open：maxAttempts 覆盖仅收紧绘本这一降级型端点，plan/report/speech 等瞬时错误敏感端点仍保持默认 3 次重试；scan.service 失败降级文案友好且无新增 re-throw 断言冲突；parent 竞态修复不改变鉴权语义)",
    "optimization": "PASSED (0 open：仅新增可选字段与两处测试，无删除/无 stub；provider 重试链路其余包装层透传 options 不变；无未用导出)"
  }
}
