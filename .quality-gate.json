{
  "phase": "feature",
  "stack": "node-ts",
  "cleared": true,
  "enforced": true,
  "reportRef": "docs/quality/ai-provider-fallback-gate.md",
  "notes": "修复 bigmodel.provider.spec.ts 三处过时断言，使其同步此前两个有意为之的源码修复（源码行为正确，不应回退）：①transcribe/assessPronunciation 改为抛 501（STT_NOT_SUPPORTED / PRONUNCIATION_NOT_SUPPORTED），让 FallbackAiProvider 继续尝试下一个 provider，否则返回空文本会被当“成功”消费 → STT 静默失败、分数恒 0；旧测试断言“不抛错返回降级结果”，现改为 rejects.toMatchObject({statusCode:501, code:...})。②synthesize 移除 response_format:'mp3'（智谱 glm-tts 收到会 400，音频兜底靠 EdgeTtsProvider），旧测试断言 body.response_format==='mp3'，现改为 toBeUndefined。验证：后端 jest bigmodel.provider.spec.ts 27/27 全绿；后端/前端 tsc --noEmit 均 0/0（Node 20，跑前须 NODE_OPTIONS= 清空被注入的 --use-system-ca）。",
  "gates": {
    "consistency": "PASSED (后端 tsc -p tsconfig.json --noEmit 0/0；前端 tsc -p tsconfig.json --noEmit 0/0；双端 tsc 均在 Node 20 通过；.quality-gate.json 随源码一并暂存)",
    "tests": "PASSED (bigmodel.provider.spec.ts 27/27 全绿；断言改为匹配 transcribe/assessPronunciation 抛 501 与 synthesize 不发 response_format；全仓 grep 确认无其它 spec 残留过时断言)",
    "review": "PASSED (0 open：仅修正单测断言以匹配既定源码契约，未改动业务源码与 secrets)",
    "optimization": "PASSED (0 open：纯测试断言更新，无删除无 stub)"
  }
}
