{
  "phase": "ai-308",
  "stack": "node-ts",
  "gates": {
    "consistency": "PASSED (frontend tsc --noEmit 0 错误; next build 通过[复用 AI-307 构建, /speech 路由 + 全部 9 页面编译]; vitest 64/64 全绿[新增 tasks.spec.ts 5 case: isSpeakingTask(icon 'mic'→true / 'headphones'→false / 'pencil'→false / 空串→false / undefined→false), speakingTaskHref(返回 '/speech?taskId=' + encodeURIComponent)]; e2e 19/19 全绿[含 AI-308 新增 speech-task-link.feature 3 scenarios]; e2e tsc 0 错误; 无新后端代码(复用 TasksModule 既有 PATCH /tasks/:id/complete + GET /tasks/daily), 既有 jest 436 tests 不退化)",
    "tests": "PASSED (unit: vitest 64/64[新增 tasks.spec.ts 5 case 全分支] + 回归 speech.spec 13 + speech-recorder.spec 17 + api.spec 11 + plan.spec 14 + logger.spec 4; e2e/bdd: speech-task-link.feature 3 scenarios 全绿[① Home 点 mic 任务→进入 /speech?taskId ② 完成会话后 Home 该任务勾选 + 完成计数 1/3 ③ 非口语任务仍一键完成]; headless harness 修复: completeSession 在返回 Home 前等 [data-component=TaskDoneNote](completeTask 异步落库信号, 10s 超时对无 taskId 会话 no-op) 消除竞态; clickSpeakingTask 从导航后 page.url() 取 taskId(Next <Link> href 经 Playwright getAttribute 返回 null 不可靠); speakingTaskId 存于共享 E2EWorld(修复 per-step HomePage 实例状态错乱导致断言恒 false))",
    "review": "PASSED (0 open; Home 口语深链[未完成的 mic 任务渲染为 <Link href=/speech?taskId> data-speech-link data-task-id, 其余维持一键完成按钮], 所有卡片带 data-task-id 供 E2E 定位; speech 页经 useSearchParams(Suspense 包裹) 读 taskId, 会话 finished 时 fire-and-forget completeTask, taskMarked 守卫保证幂等只调一次; TaskDoneNote 仅在写回 resolve 后出现, 与真实孩子'看到完成提示再回首页'行为一致; 后端 completeTask 幂等(重复完成无害) 且写 task_completions(completed 派生源) + 对计划任务回写 study_plan_days.isDone; 无新后端实体/端点/依赖, 纯前端消费既有 TasksModule; AuthGate 包裹 + 客户端导航保活 token, 规避 page.goto 重置)",
    "optimization": "PASSED (0 open; 纯逻辑 tasks.ts(isSpeakingTask / speakingTaskHref) node 直覆零 React 依赖; 无 stub/占位; 清理调试残留(移除 rawSpeakingHref / getCapturedSpeakingTaskId / getRawSpeakingHref); Home/Speech 改动最小化; completeAllTasks 增强支持口语深链任务(plan-progress 场景 future-proof), 不破坏既有一键完成路径; 无裸 console[统一 logger])"
  },
  "cleared": true,
  "enforced": true,
  "reportRef": "docs/quality/ai-308-gate.md",
  "notes": "AI-308 把每日任务中的 mic 任务联动到口语会话: Home 未完成的 mic 任务卡片深链到 /speech?taskId=..., 孩子完成跟读会话后该每日任务被勾选 + 进度回写(TasksModule completeTask 幂等写入 task_completions, getDailyTasks 派生 completed)。新增 src/lib/tasks.ts(纯逻辑 isSpeakingTask / speakingTaskHref) + tasks.spec.ts; 修改 src/app/page.tsx(mic 未完成任务改 <Link> 深链 + 全卡片 data-task-id + 复用 handleCompleteTask)、src/app/speech/page.tsx(useSearchParams 读 taskId, finished 时 completeTask 回写 + TaskDoneNote + Suspense 包裹); E2E 新增 speech-task-link.feature 3 scenarios + speech-task.steps.ts + home.ts 增强(clickSpeakingTask / isSpeakingTaskCompleted / tapFirstNonSpeakingTask / completeAllTasks 支持深链 / taskCount 计数修正)。无新后端代码/实体。验收全绿: 从 Home 任务卡直达口语页 → 完成会话 → 任务勾选 + 进度回写。E2E 修复: ① completeSession 等 TaskDoneNote 消除 completeTask 落库竞态; ② clickSpeakingTask 从导航后 URL 取 taskId(Next Link href getAttribute 不可靠); ③ speakingTaskId 存共享 World(修 per-step 实例状态错乱)。本会话反复跑 E2E 曾因共享 sqlite 后端累积状态污染出现 7 failed 假阳性, 清库重 seed + 重启后端后 19/19 稳定全绿。"
}
