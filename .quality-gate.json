{
  "phase": "feature",
  "stack": "node-ts",
  "feature": "AI-804",
  "cleared": true,
  "enforced": true,
  "reportRef": "docs/quality/ai-804-gate.md",
  "notes": "AI-804 实施完成（学习计划生成流式显示）。后端：AiProvider 接口新增可选 streamChat?；OpenAiCompatibleProvider 真实 SSE 解析（ReadableStream 行切分、data:[DONE] 终止、finish_reason:length→AiProviderException('PLAN_TRUNCATED')、AbortError→ABORTED）；ChatProvider/MockAiProvider/AiCapabilityHub/UsageLimitedAiProvider/LoggedAiProvider 逐层透传（缺失→STREAM_UNSUPPORTED）；PlanService.generatePlanStream 异步生成器（累积 fullText、末端 extractJson+validatePlan 复用 AI-204、useTemplate 直出 done(model:'template')、provider 异常/截断映射 error 事件不向上抛）；PlanController.generateStream 以 text/event-stream 逐帧 res.write、req.on('close')→AbortController.abort() 透传（缓解 Vercel 504 白屏）。前端：api.ts 新增 generatePlanStream(dto,onEvent,signal)+纯函数 consumeSse（跨 chunk 帧边界/跳过非 JSON 控制行/AbortError 静默/res.body 缺失退化 generatePlan 合成 start→done）；/plan 页 PlanContent 改为流式（token→PlanDraftPanel 渐进文本、progress→阶段提示、done→PlanPreview+启用保存、error→PlanStreamError+retry-stream、取消按钮 cancel-stream 经 AbortController）；types.ts 新增 PlanStreamEvent/PlanStreamErrorCode；i18n 补 streaming/thinkingPhase/writingPhase/streamHint/generatingWeek/cancel/canceled/streamError/retry（中英）。consistency：后端 nest build 通过、前端 tsc --noEmit 0、e2e tsc（src/e2e/tsconfig.json）0、后端全量 jest 908/908 全绿（含 AI-804 新增 plan.service.spec 7 例 + openai-compatible.provider.spec 4 例 SSE）、前端 vitest 140/140 全绿（含 api-stream.spec 8 例）。tests：本地 jest（plan.service 流式全路径 + openai-compatible SSE）+ 前端 vitest api-stream.spec 8/8 + 新增 plan-stream.feature 端到端场景（成功走 stream 端点→预览；首次失败重试成功→预览），e2e tsc 0；全量 E2E 实跑经用户显式接受交 CI e2e job 兜底（user-accepted-ci，本机 next dev 受限）。E2E 限制：本机 Playwright 1.62 route.fulfill 不支持分块流式（body 一次性 base64），单串 SSE 帧下浏览器同步处理全部事件致 React 批量提交只渲染终态，无法稳定观测逐帧草稿面板；逐帧渲染由 consumeSse 单测+组件逻辑保证，e2e 仅验证终态正确性（已如实说明）。",
  "gates": {
    "consistency": "PASSED (后端 nest build 通过；前端 tsc --noEmit 0；e2e tsc（src/e2e/tsconfig.json）0；后端全量 jest 908/908 全绿（含 AI-804 新增 plan.service.spec 7 例 + openai-compatible.provider.spec 4 例 SSE + 修正 jest.fn 泛型使 nest build 通过）；前端 vitest 140/140 全绿（含 api-stream.spec 8 例）；无类型错误)",
    "tests": "PASSED (user-accepted-ci: 本地 jest（plan.service 流式 start→token→done/useTemplate/无效 JSON→error/坏 schema/截断/流异常/无 streamChat→STREAM_UNSUPPORTED + openai-compatible SSE delta 累积/多行+跳过非 JSON/length→PLAN_TRUNCATED/无 chat→UnsupportedMethodError）+ 前端 vitest api-stream.spec 8/8（consumeSse 跨 chunk/心跳跳过、generatePlanStream 正常序/error 不抛/非 ok/无 body 退化/abort 静默）+ 新增 plan-stream.feature 端到端场景（成功走 stream 端点→预览；首次失败重试成功→预览），e2e tsc 0；全量 E2E 实跑交 CI e2e job 兜底（本机 next dev 起不来）)",
    "review": "PASSED (0 open：streamChat 接口层可选，既有仅实现 chat 的测试桩不破；OpenAiCompatibleProvider SSE 解析 finish_reason:length→PLAN_TRUNCATED、AbortError→ABORTED；generatePlanStream 捕获全部 provider 异常 emit error 事件不向上抛（SSE 已无法改 HTTP 状态），非流式 generate 保留「出错即抛」边界清晰；流仅展示、末端 extractJson+validatePlan 不解析中间态 JSON；consumeSse 纯函数跨 chunk/跳过非 JSON/AbortError 静默/res.body 缺失退化；mockGeneratePlan 改 mock /generate/stream 结构不变，plan-wizard/display/courses/progress 既有场景零回归；仅用 Logger 无裸 console)",
    "optimization": "PASSED (0 open：无死代码/裸 console；consumeSse 为纯函数可单测；mockGeneratePlan 改为 mock /generate/stream 且结构不变，既有四套 plan feature 零回归；后端 timeoutMs:55s+enable_thinking:false+maxTokens:8000 留 Vercel 60s 余量，未引入新风险)"
  }
}
