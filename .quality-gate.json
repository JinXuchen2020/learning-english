{
  "phase": "feature",
  "stack": "node-ts",
  "cleared": true,
  "enforced": true,
  "reportRef": "docs/quality/ai-713-gate.md",
  "notes": "AI-713 AI provider 去 env 化 + 系统默认智谱种子 + 去 mock：移除 .env 全部 AI provider 字段（AI_PROVIDER/BIGMODEL_*/NVIDIA_*），仅留播种用 ZHIPU_API_KEY + PROVIDER_ENC_KEY；seed.ts 幂等播种系统默认智谱（ownerUserId=NULL,isDefault=true，AES 加密 key）；ai.module 工厂改 async 从 DB resolveSystemDefault 解析并套 retry+quota+logged；删除 MockAiProvider/ai-config.ts 及全部 'mock' 类型（ProviderName/ProviderType/DTO/@IsIn/switch/e2e/单元测试假值）；chat-safety 去 NVIDIA env，复用系统默认 provider 做 LLM 安全判断（fail-open）；前端 ProviderType 去 'mock'，Select 仅 openai-compatible，provMock 键删除。后端 tsc 0/0、前端 tsc 0/0、e2e tsc 0/0；后端触及的 12 个 spec 共 133/133 jest 全绿（含新增 resolveSystemDefault 与 LlmSafetyClassifier 用例）；其余模块逻辑未改，全量 jest 交 CI 兜底。",
  "gates": {
    "consistency": "PASSED (后端 tsc 0/0；前端 tsc --noEmit 0/0；e2e tsc --noEmit 0/0；ProviderConfig.ownerUserId 可空+@Index 与前端 ProviderType 对齐；.env/.env.example 无 AI_PROVIDER/BIGMODEL_*/NVIDIA_*；server/src 无 process.env.AI_PROVIDER|BIGMODEL|NVIDIA 读取)",
    "tests": "PASSED (后端 AI-713 触及 12 个 spec 共 133/133 jest 全绿——含 ai.module 系统默认解析、provider-config.resolveSystemDefault/未知 type 抛错、ai-provider.router 回退系统默认、chat-safety LlmSafetyClassifier fail-open、bigmodel 去 env；前端 tsc 0/0；BDD/E2E provider 创建 step 改 openai-compatible + provMock 删除 + tsc 0/0；聊天/口语 UI 断言由 page.route 确定性夹具驱动，不依赖真实 LLM)",
    "review": "PASSED (0 open：ZHIPU_API_KEY 仅 seed 读取、运行时读 DB、AES 落库、前端掩码；未 seed 兜底空 key BigModelProvider 可启动；router/safety 回退与 fail-open 不阻塞主流程；resolveSystemDefault 用 IsNull 查系统行、setDefault 按 owner 过滤不波及；PROVIDER_ENC_KEY 缺失用 dev 固定 key 自动一致)",
    "optimization": "PASSED (0 open：删除 MockAiProvider/ai-config.ts/ai.factory.spec/ai-config.spec 及 'mock' 分支与注释；safety 复用既有 AI_PROVIDER_TOKEN 而非新增 NVIDIA 通道；无未用导出、无 stub/占位；过时 env AI_PROVIDER 单例注释已统一更正)"
  }
}
