{
  "phase": "feature",
  "stack": "node-ts",
  "cleared": true,
  "enforced": true,
  "reportRef": "docs/quality/ai-provider-fallback-gate.md",
  "notes": "AI-801 定制计划→生成配套课程（路径 B）：新增 features/ai-801.md 与 backlog M9 里程碑，定义 POST /api/ai/plan/:id/generate-courses、CoursesService.createCourseFromPlan、courses-from-plan 提示词/校验/降级模板契约；.gitignore 增补排障临时产物（_be_probe.json、_fe_probe.html、_be_diag*.log、server/diag-boot.js、server/.tts_cache/、server/_testtts.js）。本提交同时并入此前已重启验证的语音跟读修复：浏览器 Web Speech API 预转写（clientTranscript 可选旁路，绕过云端 STT 全不通）+ 聊天参考文本清洗（extractEnglishForReadAlong、stripMetaMarkers）去除系统标记与中文翻译；BigModelProvider.transcribe/assessPronunciation 改为抛 501 触发前端预转写；新增 EdgeTtsProvider 作为 synthesize 链末兜底（本地 edge-tts 免费微软 TTS）；新增 edge-tts.provider、chat-text.util 及对应 spec。",
  "gates": {
    "consistency": "PASSED (后端 tsc -p tsconfig.json --noEmit 0/0；前端 tsc -p tsconfig.json --noEmit 0/0；.gitignore 增补临时产物排除；.quality-gate.json 随源码一并暂存)",
    "tests": "PASSED (后端 jest chat-text.util + text-similarity.util 共 2 套件 30/30 全绿；其余模块沿用既有 spec)",
    "review": "PASSED (0 open：API key 仍经 AGNES_API_KEY/ZHIPU_API_KEY 环境变量注入未硬编码；EdgeTtsProvider 仅作 TTS 兜底不改变 chat 语义；clientTranscript 为可选旁路，缺省走原 STT 链)",
    "optimization": "PASSED (0 open：仅新增 provider/util/spec 与文档，无删除无 stub；EdgeTtsProvider 构造同步探测 python 并仅兜底 synthesize)"
  }
}
