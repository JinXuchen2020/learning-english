{
  "phase": "ai-301",
  "stack": "node-ts",
  "gates": {
    "consistency": "PASSED (tsc --noEmit 0 错误; nest build 通过; jest 全绿 46 suites/333 tests; ai_speech_attempts 由 in-memory better-sqlite3 synchronize 自动建表成功, 无 DataTypeNotSupportedError; 纯后端无全栈契约)",
    "tests": "PASSED (unit: 后端 jest AI-301 2 文件共 17 case 全绿[ai-speech-attempt.service.spec.ts 13: clampScore(NaN/Infinity/undefined→0, <0→0, >100→100, 小数四舍五入) + sanitizePhonemes(null/[]→[], trim+过滤空串) + record(成功透传/越界钳制/DB失败 best-effort 返回 false 且 warn) + findByUser(order DESC/take=limit); ai-speech-attempt.entity.spec.ts 4: 建表默认值/wordId·sentenceId 空/simple-array 空数组与多元素 round-trip/record 越界 score 经 clampScore 兜底/倒序查询]; e2e/bdd: 0 — 纯后端数据模型 + 落库服务, 无 HTTP 路由、无前端 UI 旅程, 不为纯后端写 BDD, 设计文档显式标注豁免, 口语 E2E 用户旅程随 AI-307 /speech 页交付)",
    "review": "PASSED (0 open; 空安全[userId 必填, wordId/sentenceId 可空且 record 经 ?? null 兜底]; score 钳制兜底[clampScore 防上游越界脏数据]; best-effort 吞异常不阻断主流程[record DB 失败仅 logger.warn 返回 false]; simple-array 双驱动可移植 sqlite/postgres; 时间列铁律[@CreateDateColumn, 无 @Column(timestamp)]; 无裸 console[统一 logger]; 与 AiCallLog(AI-108) 风格一致; userId 非硬 FK 审计型追加记录已在设计文档风险点说明)",
    "optimization": "PASSED (0 open; 无 stub/占位; clampScore/sanitizePhonemes 纯函数复用且导出供单测直覆; repo.find 单次查询含 order+take 无 N+1; 生产代码无裸 console; 无临时调试残留)"
  },
  "cleared": true,
  "enforced": true,
  "reportRef": "docs/quality/ai-301-gate.md",
  "notes": "AI-301 是 M3 口语训练的数据底座：新增 AiSpeechAttempt 实体(ai_speech_attempts 表: userId/wordId/sentenceId/audioPath/score/weakPhonemes/createdAt) + AiSpeechAttemptService(最佳努力落库 record + 按用户倒序查询 findByUser), 注册进 AiModule 与 appEntities, 供 AI-303/306/307 消费。落库层对 score 做 clampScore[0,100] 钳制、对 weakPhonemes 做 sanitizePhonemes 清洗兜底。DB 写入失败仅告警不阻断(与 AI-108 同口径)。测试沿用 AI-201 实体-first 约定: 单元测试(2 文件 17 case)全绿, E2E 因纯后端无用户旅程豁免(设计文档显式标注)。回归: 更新 ai.module.spec 桩 AiSpeechAttempt 仓库以适配新 DI 依赖, 全量 333 tests 全绿, 覆盖率 90/70 基线不退化。"
}
