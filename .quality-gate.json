{
  "phase": "ai-209",
  "stack": "node-ts",
  "gates": {
    "consistency": "PASSED (tsc --noEmit 0 错误; vitest 29/29 全绿[lib/plan.spec 14 + lib/api.spec 11 + lib/logger.spec 4]; next build 含 /plan 路由 OK; 前后端字段对齐[前端 PlanStatusResponse{hasPlan,totalDays,doneDays,completionRatio,planId?,appliedAt?} ↔ 后端 PlanStatusResult; 前端 getPlanStatus(childId) → GET /api/ai/plan/status?childId= ↔ 后端 PlanController GET status + PlanService.getPlanStatus; 回写 childId 一致性: 前端 save/apply/getPlanStatus 全程用 useAuth().user.id, 与后端 study_plans.userId 对齐]; 前端纯展示页面由 E2E 覆盖, 无全栈契约漂移)",
    "tests": "PASSED (unit: 后端 jest 31/31[server/src/tasks/tasks.service.spec.ts 新增 completeTask 回写分支: plan 任务带 planDayId→回写 isDone=true / 非 plan 任务(planDayId 为空)→不回写 / 已完成幂等(completeTask 已存在直接返回不二次回写) + plan.service.spec.ts 新增 getStatus: 有 applied 计划→hasPlan=true 统计 done/total / 无 applied 计划→hasPlan=false]; 前端 vitest 11→29[lib/api.spec.ts 新增 getPlanStatus 成功解析 PlanStatusResponse / 解析空响应兜底 共 2 case, 累计 11]; e2e/bdd: plan-progress.feature 2 scenarios[应用计划后 Home 展示 PlanProgress 完成度 0/N / 完成所有每日任务后 PlanProgress 完成度递增] 全绿, 全量 E2E 套件 13 scenarios/121 steps 全绿, 浏览器走系统 Edge 免 Chromium 下载; 免 key 经 MockProvider 降级 degraded:true 计划仍有效可 apply)",
    "review": "PASSED (0 open; 空安全[doneDays = days.filter(d=>d.isDone).length 对 plan.days ?? [] 防护 / completionRatio 分母 0 兜底]; 回写幂等[TasksService.completeTask 仅当 task.planDayId 存在才回写 StudyPlanDay, 避免污染全局种子任务]; 错误映射[getPlanStatus 失败被 load() try/catch 吞掉仅记 logger, 不阻断首页其余数据]; 复用 AI-206 replacePlanTasks 写入的 planDayId 关联, 无重复造轮子; 数据契约 childId 全程一致; data-component 钩子一致 Home/GreetingBanner/PlanProgress/DailyTasks/CourseProgress; ProgressRing 环形进度纯展示组件无副作用; AuthGate 包裹鉴权流)",
    "optimization": "PASSED (0 open; 无 stub/调试残留; getStatus 单 findOne + relations(['days']) 一次查询统计完成度, 无 N+1; 首页并行 Promise.all 拉取 courses/tasks/progress/planStatus, completeTask 后并行刷新 progress+planStatus; 生产代码无裸 console; PlanProgress 仅在 hasPlan 时渲染, 无计划用户零开销)"
  },
  "cleared": true,
  "enforced": true,
  "reportRef": "docs/quality/ai-209-gate.md",
  "notes": "AI-209 是 M2 学习计划闭环的最后一环：完成计划内任务时后端回写 study_plan_days.isDone, 并在 Home 展示计划完成度卡。后端 TasksService.completeTask 注入 StudyPlanDay 仓库, 完成任务且该 task 带 planDayId 时幂等回写 isDone=true(复用 AI-206 replacePlanTasks 写入的 planDayId 关联); PlanService 新增 getPlanStatus(childId) 取最近 applied 计划统计 done/total, PlanController 新增 GET status?childId=。前端 page.tsx 加载时并行拉取 getPlanStatus, 仅 hasPlan 时渲染 data-component=PlanProgress(ProgressRing + 已完成 X/Y 天), handleCompleteTask 成功后并行刷新 planStatus。BDD/E2E 必做(约束 #6): plan-progress.feature 2 scenarios / 13 total scenarios 121 steps 全绿, 导航经 client-side TabNav 点击保 token。踩坑: E2E 失败根因为 next start 起的是陈旧生产构建(.next 不含 PlanProgress), 重建 + 重启后全绿。"
}
