{
  "phase": "feature",
  "stack": "node-ts",
  "cleared": true,
  "enforced": true,
  "reportRef": "docs/quality/ai-provider-fallback-gate.md",
  "notes": "AI-713 排查盲区 + 种子 key 自愈：(1)根因——ai_call_logs 三处盲区使线上失败无法定位：①provider 列硬编码 'bigmodel'（无法区分 Agnes/Zhipu）；②moduleTag 恒为 'global'（无法按 chat/transcribe 归因）；③errorMessage 截断 255 且无 stack，上游真实错误被掩盖，DB 取证只能看到「未配置/无效的令牌」却不知哪个 provider。另：ensureProviderConfigs 对已存在系统 provider 直接跳过，坏/过期 key 永不自愈（运维陷阱）。(2)修复——①AiProvider.name 由 ProviderName 联合类型放宽 string，router/bigmodel/openai-compatible/retryable/usage-limited/logged/fallback 全部透传真实 provider 名（来自 DB ProviderConfig.name，如「智谱 GLM (系统默认)」「Agnes AI」）；②ModuleTagResolver 签名改为 (operation)=>string，AI_MODULE_TAG_RESOLVER_TOKEN 默认 (op)=>op，logged-ai-provider 以 operation 作为 moduleTag 落库；③ai-call-log.entity 增 errorStack(TEXT) 列，logged-ai-provider 捕获 errorStack 透传，ai-call-log.service.record 持久化 errorStack(截断4000)，fallback-ai-provider.tryChain 聚合每个 provider 失败明细为 message（[AI 兜底链 X] 全部失败 → A: msg | B: msg）并保留末错误类型/statusCode；④ensureProviderConfigs 对已存在系统 provider 按 env 刷新 apiKeyEnc（agnes/zhipu），坏 key 自愈＋智谱历史默认降级为兜底。(3)回归测试——fallback-ai-provider.spec(逐 provider 明细)、bigmodel.provider.spec(name 透传默认 bigmodel/真实名)、bootstrap-seed.spec(env key 轮换刷新)、text-similarity.util.ts StrategySelection.providerName 放宽 string。(4)验证——后端 tsc 0/0（Node 20）；jest 62/62（6 suites：fallback-ai-provider、bootstrap-seed、bigmodel.provider、ai.module、logged-ai-provider、ai-call-log.service）；实测真实 chat 经 fallback 链，Agnes 当前 key 失效（无效的令牌）被精确识别、智谱兜底返回 Hello，验证 observability + fallback 生效。 Agnes 端点切换（用户换 key+网关）：seed.ts 的 Agnes baseUrl 改为 env 驱动（AGNES_API_URL，默认 https://apihub.agnes-ai.com/v1），旧 api.agnes-ai.cn 已失效；新 key 经 env 注入（.env，已 gitignore），dev.sqlite 现有 Agnes 行 baseUrl 已手动更新为 apihub/v1，重启后端由 ensureProviderConfigs 按 env 刷新 apiKeyEnc 自愈（实测新 key 经 /v1 返回 Hello，根路径 403）。 Vercel(Postgres)部署修复：synchronize 在已有 ai_call_logs 表上加 NOT NULL 报「column provider contains null values」——根因 ai-call-log.entity 的 provider 列未显式 nullable，TypeORM 默认 NOT NULL，而历史行有 NULL。修复：provider 列改 nullable:true（新写入恒定非空，仅兼容历史行），errorStack 本就 nullable。tsc 仍 0/0。 生产 schema 管理切到 migration：新增 server/src/migrations/InitSchema.ts（基线，复用 TypeORM Table.create(metadata,driver) 生成与 synchronize 一致且 driver 正确的 DDL；两阶段——先建表不含 FK(ifNotExist 幂等)，再按「引用表+引用列」补 FK，兼容历史 synchronize 约束名差异，对线上已存在的库为安全 no-op）；database.config.buildDataSourceOptions 改为生产(Postgres)默认 synchronize:false + migrationsRun:true（DB_SYNCHRONIZE=true 可临时回退），dev(sqlite)保留 synchronize:true 不跑 migration；新增 server/ormconfig.ts 供 npm run typeorm -d ormconfig.ts migration:generate 生成后续迁移（Postgres DDL 须连真实 Postgres 生成）。验证：tsc 0/0；nest build 产出 dist/migrations/InitSchema.js；自写校验脚本对全新 sqlite(27 表创建)与 dev.sqlite 副本(线上库模拟，no-op)均幂等通过；应用以新构建启动并正常返回 chat。",
  "gates": {
    "consistency": "PASSED (后端 tsc -p tsconfig.json --noEmit 0/0（Node 20）；frontend 未改动；.quality-gate.json 随源码一并暂存)",
    "tests": "PASSED (后端 jest 62/62：fallback-ai-provider.spec、bootstrap-seed.spec、bigmodel.provider.spec、ai.module.spec、logged-ai-provider.spec、ai-call-log.service.spec；tsc 0/0；含 AI-713 回归用例：逐 provider 错误明细 / name 透传 / env key 轮换刷新)",
    "review": "PASSED (0 open：仅 provider 名透传 + moduleTag 按 operation + errorStack 落库 + 种子 key 刷新；未改动业务契约与 secrets；provider 名放宽 string 为 AI-713 排查需要)",
    "optimization": "PASSED (0 open：无删除无 stub；真实 provider 名透传与 errorStack 落库均为可观测性修正)"
  }
}
