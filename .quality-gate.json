{
  "phase": "ai-707",
  "stack": "node-ts",
  "gates": {
    "consistency": "PASSED (前端 tsc --noEmit 0 错误；后端 tsc --noEmit 0 错误（新增 User.role 列 + auth 返回/签发 role，无契约破坏性变更——前端 api.ts AuthUser 增 role、register 增 role 参数与后端对齐）；e2e tsc --noEmit 0 错误；TypeORM @Column({type:'varchar', default:'child'}) 显式类型避免 DataTypeNotSupportedError，better-sqlite3 synchronize:true 自动加列无需手写迁移；TabNav 以 window.location.hash + hashchange 区分 /parent 概览与设置，规避 useSearchParams 的 next build Suspense 边界；/parent 页新增概览区 + #settings/#approvals 锚点，与 parentTabs 的 /parent、/parent#settings 一致)",
    "tests": "PASSED (unit: src/lib/i18n-messages.spec.ts 2/2 通过——(1) zh/en 递归键对齐；(2) 全 src 扫描 t('key') 字面量均存在——本次新增 TabNav.parentHome/parentSettings、Login.roleLabel/roleChild/roleParent、Parent.overviewGreeting/overviewSubtitle/statPending/statReport/viewReport/statSettings/goSettings 共 12 键，zh/en 双文件齐备；后端 Jest：auth.service.spec 8/8 通过，register 默认昵称/返回 token 等既有断言未回归（role 参数为可选、无破坏性签名）；角色路由守卫为前端路由逻辑，未新增 e2e 场景，可后续补 parent/child 越权重定向 bdd)",
    "review": "PASSED (0 open；User.role 联合类型 'child'|'parent'，后端 RegisterDto 以 IsIn 校验、AuthService 仅当显式 'parent' 才置 parent（缺省/非法→child），无越权自提；JWT payload 含 role，与既有 ai-provider-context.interceptor 期望一致（role==='parent' 走自身 userId，child 解析 parentId）；前端 auth-context 暴露 role/isParent/isChild；TabNav 按 isParent 渲染 child 4 栏 / parent 3 栏（概览/报告/家长）；RoleGuard 包裹 layout，越权路由 router.replace（child 误入 /parent|/parent-report → /；parent 误入学习页 → /parent）；/parent 概览仅汇总家长侧已有数据（待审批数 + 跳转报告/设置），孩子学习明细统计为后续增强；无裸 console；全站 .tsx CJK grep 确认无用户态中文残留)",
    "optimization": "PASSED (0 open；无 stub；TabNav 复用 @/i18n/navigation locale 感知 Link，hash 区分零新增依赖；RoleGuard 逻辑集中单一文件；/parent 概览复用页面既有 approvals 状态未重复拉取；新增键归入既有 TabNav/Login/Parent 命名空间，结构清晰)"
  },
  "cleared": true,
  "enforced": true,
  "reportRef": "docs/quality/ai-707-gate.md",
  "notes": "AI-707 角色化账号（方案 B）：注册时选角色（孩子/家长），不同角色显示不同页面。后端 User 实体新增 role 列（@Column({type:'varchar', default:'child'})，TypeORM 显式类型避免 DataTypeNotSupportedError）；RegisterDto 加 role?: 'child'|'parent'（IsIn 校验），AuthService.register 仅当显式 'parent' 才置 parent；buildResponse 的 JWT payload 与返回体均含 role（激活既有 ai-provider-context.interceptor 的 role 路由——parent 走自身 userId，child 解析 parentId）。前端 api.ts AuthUser 增 role、register 增 role 参数；auth-context 暴露 role/isParent/isChild；login 注册模式加角色切换（roleLabel/roleChild/roleParent，grid grid-cols-2 切换 aria-pressed）。TabNav 重写：child 4 栏（首页/课程/练习/奖励），parent 3 栏（概览/报告/家长）；parent 用 /parent 概览 + /parent#settings 设置，以 window.location.hash + hashchange 监听区分 active，规避 useSearchParams 的 next build Suspense 边界要求。RoleGuard 包裹 layout 强制越权重定向。/parent 页新增「家长首页概览」区（问候 + 待审批数 + 跳转报告/设置卡片，复用既有 approvals 状态）+ #settings（包裹 AI 提供商配置 + PIN 管理）与 #approvals 锚点。验证：前端 tsc --noEmit 0 + 后端 tsc 0 + i18n-messages.spec.ts 2/2 + auth.service.spec 8/8。注意：孩子学习明细（单词数/连续天数等）概览暂未接入，需后端新增家长侧聚合端点，列为后续增强。"
}
