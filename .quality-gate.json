{
  "phase": "ai-603",
  "stack": "node-ts (NestJS 10 + TypeORM + better-sqlite3 / Next.js 14)",
  "gates": {
    "consistency": "PASSED (后端 tsc -p tsconfig.json --incremental false 全量类型检查 EXIT 0/0 错误; 前端 next build 类型检查 0 错误[12 路由编译成功]; 全栈契约对齐: MascotLevelInfo/MascotStory 两端一致[server mascot-level.util.ts 与 src/lib/mascotLevel.ts + src/lib/types.ts 同口径 computeLevel/LEVEL_THRESHOLDS/MAX_LEVEL], GET /ai/mascot/level 与前端 getMascotLevel 对齐, GET /ai/mascot/story/:level 与前端 getMascotStory 对齐; User.level 列 type:'int' 显式; MascotStory 实体加入 appEntities + forFeature)",
    "tests": "PASSED (unit: 后端 mascot-story.service.spec 9 PASS[getLevelInfo 阈值推导/缺用户默认 L1/250星推导L4/999星满级 + getOrGenerateStory 幂等不调AI/首次AI生成/AI抛错降级/非法JSON降级/level<1钳到1, mock repos + mock AiProvider]; 前端 lib/mascotLevel.spec 4 PASS[computeLevel 阈值边界/满级/MAX_LEVEL长度 + buildLevelInfo 进度]; e2e/bdd: mascot-story.feature 1 scenario/5 steps 全绿[登录新用户→看到等级成长卡→吉祥物 data-level=1→点看成长故事→弹层标题+文案非空], 成长卡 data-component=MascotGrowthCard + 按钮 ViewGrowthStoryBtn + 弹层 MascotStoryModal/Title/Text 可断言, step 文本唯一无 ambiguous, 普通 button 点击无 Next.js Link 卸载竞态)",
    "review": "PASSED (0 open; getOrGenerateStory 幂等[已有直接返回] + 唯一约束 race 回查; AI 失败/解析失败降级模板 isDefault:true 不 5xx; computeLevel 单一真相 + completeLesson 星星+1 后重算 User.level; Mascot 按 level 渲染配饰[围巾/披风/光环/皇冠] data-level 钩子; MockProvider 新增剧情关键词+fixture 保证无 key 环境可演示; 无裸 console)",
    "optimization": "PASSED (0 open; computeLevel/buildLevelInfo 纯函数前后端同口径单一真相; 按需生成[前端调 getOrGenerateStory]避免 completeLesson 同步 AI 阻塞学习写入; 无 stub; 降级路径真实可用[MockProvider 返回合法 JSON 或解析失败走模板])"
  },
  "cleared": true,
  "enforced": true,
  "reportRef": "docs/quality/ai-603-gate.md",
  "notes": "AI-603 = 吉祥物成长剧情(等级由累计星星推导 + 按需生成温暖鼓励剧情 + 吉祥物随等级渲染配饰 + Home 等级环与看成长故事弹层 + E2E). 后端: mascot-level.util.ts[computeLevel/buildLevelInfo/LEVEL_THRESHOLDS=[0,50,120,200,300,500]/MAX_LEVEL=6]; mascot-story.entity[MascotStory, @Unique(userId,level)]; mascot-story-agent[MASCOT_STORY_SYSTEM_PROMPT/parseMascotStoryOutput 鲁棒解析/DEFAULT_STORY]; mascot-story.service[getLevelInfo/getOrGenerateStory 幂等+降级+唯一约束race]; mascot-story.controller[GET level/story/:level]; user.entity 加 level 列; progress.service completeLesson 重算 level; mock-ai.provider 加 STORY_KEYWORDS+MOCK_STORY_TEXT; ai.module/database.config 注册. 前端: lib/mascotLevel.ts[同口径]+spec; types 加 MascotLevelInfo/MascotStory; api 加 getMascotLevel/getMascotStory; Mascot.tsx 加 level prop+配饰; app/page.tsx 等级环+成长卡+弹层. E2E: mascot-story.feature 1 scenario/5 steps + pages/mascot.ts + steps. 关键环境修复: 1)后端 jest 用 jest/bin/jest.js 入口[.bin/jest 是 bash 脚本不能 node 直跑]; 2)tsc 用 typescript/lib/tsc.js 入口[.bin/tsc 是 bash 脚本], spec 的 create mock 改用 (p?:any) 满足 Repository.create 零参重载; 3)E2E 前必须杀掉占用 :4000/:3000 的上次会话遗留 server[pkill/端口占用致 EADDRINUSE, 旧 server 无 mascot 路由→404]; 4)server/.env 默认 AI_PROVIDER=bigmodel 无 key 致真实调用返回空→降级, E2E 后端改用 AI_PROVIDER=mock 启动走 MockProvider fixture[isDefault:false]; 5)E2E 用 next build+start 生产模式(:3000)替代 next dev(按需编译卡死整台 server). 验证: tsc --incremental false 0错; mascot-story.service.spec 9 PASS; mascotLevel.spec 4 PASS; next build+start :3000 listening + node dist/main.js :4000[/ai/mascot/level 200, /ai/mascot/story/1 200 mock fixture]; cucumber mascot-story 1 scenario/5 steps 绿. 四质量门 + 提交(不 push)."
}
