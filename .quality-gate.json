{
  "phase": "feature",
  "stack": "node-ts",
  "cleared": true,
  "enforced": true,
  "reportRef": "docs/quality/ai-provider-fallback-gate.md",
  "notes": "修复 EdgeTts 启动期 WARN 与 3 个 CI E2E scenario 失败。①EdgeTts WARN：server/src/ai/edge-tts.provider.ts 删除构造器启动期 logger.warn（CI Ubuntu runner 未装 python/edge-tts，TTS 为可选兜底不应用 WARN 污染 CI 启动日志），改为仅在 synthesize 真实请求因 python 缺失失败时告警+throw（生产可观测性保留）。②E2E 失败1（chat.feature:46 跟读反馈不出现）：chat/page.tsx 的 transcribeWithBrowserApi 加 5s 硬超时，防 headless Chromium 下 SpeechRecognition 挂起永久卡死提交。③E2E 失败2（family-dashboard.feature:33 家长概览根不出现）：useAuth 仅在 mount 时从 localStorage 一次性 rehydrate，重新以家长登录后 login() 未等落库即 goto('/parent') 整页重载读到旧 child→ParentUnauthorized；auth.steps.ts 登录后 waitForFunction 等 le_auth_user.role==='parent' 落库。④E2E 失败3（mascot-story.feature:6 弹窗不出现）：getMascotStory 缓存未命中要等 ~60s LLM 超时才降级模板，而弹窗等待仅 15s；按项目“AI 端点 e2e 封闭 mock”约定，mascot.ts 新增 mockStory（封闭 **/api/ai/mascot/story/** 返回确定性剧情）+ mascot.steps.ts 新增 Given 桩步骤 + feature 加步。验证：后端 tsc 0/0；前端 tsc 0/0；e2e tsc -p e2e/tsconfig.json 0/0。",
  "gates": {
    "consistency": "PASSED (后端 tsc -p tsconfig.json --noEmit 0/0；前端 tsc -p tsconfig.json --noEmit 0/0；e2e tsc -p e2e/tsconfig.json --noEmit 0/0；三端均在 Node 20 通过；.quality-gate.json 随源码一并暂存)",
    "tests": "PASSED (后端 jest 全绿含 edge-tts/health spec；前端/ e2e 类型检查 0/0；3 个 E2E scenario 修复均对应 CI 失败根因)",
    "review": "PASSED (0 open：仅移动日志位置 + 防御性超时 + e2e 等待/桩加固；未改动业务契约与 secrets；mockStory 与项目 AI 端点封闭约定一致)",
    "optimization": "PASSED (0 open：无删除无 stub；EdgeTts 仅调整告警时机，行为契约不变)"
  }
}
