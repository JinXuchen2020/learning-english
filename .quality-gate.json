{
  "phase": "ai-801-hardening",
  "stack": "node-ts",
  "feature": "AI-801",
  "cleared": true,
  "enforced": true,
  "reportRef": "docs/quality/ai-801-hardening-gate.md",
  "notes": "AI-801 课程生成链路加固 + 配套体验修复：① retryable-ai-provider 超时环境变量化（AI_COURSE_TIMEOUT_MS，默认 18s×3 < Vercel 60s）+ maxTokens 12000 治理 JSON 截断降级；② generate-courses 幂等（同计划重复生成直返已有课程 model:idempotent，实测 85s→116ms 零 AI 调用；种子课程不误判、引用课程被删回退重生成），plan.service 新增 findExistingGeneratedCourse + 3 spec 用例；③ 前端计划页异常路径 loading 卡死修复；④ api.ts 401 统一处理（清会话跳 /{locale}/login，登录豁免/防循环/multipart+SSE 出口覆盖）+ api-401.spec 5 用例；⑤ 练习页跟课模式「返回课程」按钮（仅 courseId 存在时显示，tab 自由练习不显示）。详见 docs/quality/ai-801-hardening-gate.md。",
  "gates": {
    "consistency": "PASSED (前端 tsc --noEmit 0；后端 tsc --noEmit 0；i18n zh/en key parity 通过)",
    "tests": "PASSED (user-accepted-ci: 后端 jest 933/933 全绿（100 suites，Node 22 运行时——better-sqlite3 为 ABI 127，真实 SQLite 的 6 个 suite 在系统 Node 24 下因 NODE_MODULE_VERSION 137 不匹配失败，属环境问题非代码回归）；前端 vitest 160/160 全绿（17 files，含 api-401 5 用例与 i18n parity）；实测幂等二次调用 116ms 无 AI-CALL；E2E 本地未跑，交 CI e2e job 兜底)",
    "review": "PASSED (0 open：幂等判据双守卫——lessonRefsJson 缺失回退 content 解析、findOne NotFound 视为未生成可重建；401 仅对携带 token 的请求触发，登录/注册凭据错误不受影响；练习提前退出不计完成不加星，与完成判定一致)",
    "optimization": "PASSED (0 open：幂等检查=O(计划天数) 引用解析+至多一次 findOne，命中路径零 AI 调用；零新增依赖；改动面最小化)"
  }
}
